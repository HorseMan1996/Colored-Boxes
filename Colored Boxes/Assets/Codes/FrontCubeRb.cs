using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FrontCubeRb : MonoBehaviour
{
   /** public static bool finish = false;
    Rigidbody rb;
    GameObject FrontCube;
    private void Start()
    {
        FrontCube = this.gameObject;
    }
    // Update is called once per frame
    void Update()
    {
        RbOn();
    }

    public void RbOn()
    {
        if (finish)
        {
            finish = false;
            FrontCube.gameObject.AddComponent<Rigidbody>();
            rb = GetComponent<Rigidbody>();
        }
    }*/
}
